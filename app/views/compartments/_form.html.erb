<%= form_for(@compartment) do |f| %>
  <% if @compartment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@compartment.errors.count, "error") %> prohibited this compartment from being saved:</h2>

      <ul>
      <% @compartment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :capacity %><br>
    <%= f.number_field :capacity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
